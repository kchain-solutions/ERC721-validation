{
  "name": "erc721-validation",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ simple-blog",
    "create-local": "graph create --node http://localhost:8020/ simple-blog",
    "remove-local": "graph remove --node http://localhost:8020/ simple-blog",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 simple-blog",
    "test": "graph test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.1",
    "hardhat": "^2.12.6",
    "@graphprotocol/graph-cli": "0.37.7",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.8.1",
    "dotenv": "^16.0.3",
    "matchstick-as": "0.5.0"
  }
}